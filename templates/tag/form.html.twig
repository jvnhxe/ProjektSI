{% extends 'base.html.twig' %}
{% block body %}
    <h1>{{ title }}</h1>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-primary mt-2">"{{ 'action.save'|trans }}"</button>
    <a href="{{ path('tag_index') }}" class="btn btn-link">"{{ 'action.back_to_list'|trans }}"</a>
    {{ form_end(form) }}
{% endblock %}
